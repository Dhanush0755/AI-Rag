# AI-Rag

AI-RAG (Retrieval Augmented Generation) is an AI approach that combines information retrieval with Large Language Models (LLMs) to generate accurate responses. It retrieves relevant data from a knowledge base and provides it as context to the LLM before generating an answer.
